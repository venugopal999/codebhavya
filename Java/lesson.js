"use strict";
const JAVA_KEY="codebhavya.java.course.progress.v1";
const levelNumber=Math.min(24,Math.max(1,Number(new URLSearchParams(location.search).get("level"))||1));
const lesson=window.JAVA_LESSONS[levelNumber];
const esc=value=>String(value??"").replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch]));
const rich=value=>esc(value).replace(/`([^`]+)`/g,"<code>$1</code>");
const allLevels=window.JAVA_OUTLINE.flatMap(part=>part.levels.map(level=>({part:part.name,n:level[0],title:level[1],desc:level[2]})));
function readDone(){try{return JSON.parse(localStorage.getItem(JAVA_KEY)||"[]").map(Number)}catch(_){return[]}}
function renderSidebar(){const host=document.getElementById("levelNavigation");host.innerHTML=window.JAVA_OUTLINE.map(part=>`<div class="sidebar-part">${esc(part.name)}</div>${part.levels.map(([n,title])=>`<a href="lesson.html?level=${n}" data-search="${esc(title.toLowerCase())}" class="${n===levelNumber?"active":""}">${String(n).padStart(2,"0")} ${esc(title)}</a>`).join("")}`).join("");const input=document.getElementById("levelSearch");input.addEventListener("input",()=>{const q=input.value.trim().toLowerCase();host.querySelectorAll("a").forEach(a=>a.hidden=!!q&&!a.dataset.search.includes(q));host.querySelectorAll(".sidebar-part").forEach(h=>{let next=h.nextElementSibling,found=false;while(next&&!next.classList.contains("sidebar-part")){if(next.tagName==="A"&&!next.hidden)found=true;next=next.nextElementSibling}h.hidden=!found;});});}
function sectionHtml(section,index){const table=section.table?`<div class="concept-table-wrap"><table class="concept-table"><thead><tr>${section.table.headers.map(h=>`<th>${rich(h)}</th>`).join("")}</tr></thead><tbody>${section.table.rows.map(row=>`<tr>${row.map(cell=>`<td>${rich(cell)}</td>`).join("")}</tr>`).join("")}</tbody></table></div>`:"";return `<article class="concept-block"><h3>${index+1}. ${esc(section.title)}</h3>${section.paragraphs.map(p=>`<p>${rich(p)}</p>`).join("")}${section.bullets?.length?`<ul>${section.bullets.map(x=>`<li>${rich(x)}</li>`).join("")}</ul>`:""}${table}${section.note?`<div class="concept-note"><strong>Important:</strong> ${rich(section.note)}</div>`:""}</article>`;}
function examplesHtml(){return lesson.examples.map((example,index)=>`<article class="example-card"><header><div><span>EXAMPLE ${String(index+1).padStart(2,"0")}</span><h3>${esc(example.title)}</h3></div><button class="copy-code" type="button" data-copy-example="${index}">Copy code</button></header><div class="code-wrap"><pre><code>${esc(example.code)}</code></pre></div><footer>${rich(example.explanation)}${example.output?`<pre>Output:\n${esc(example.output)}</pre>`:""}</footer></article>`).join("");}
function visualHtml(){if(!lesson.visual)return"";return `<section class="lesson-section" id="trace"><header class="section-heading"><span>INTERACTIVE LEARNING · PROGRAM TRACE</span><h2>${esc(lesson.visual.title)}</h2><p>${rich(lesson.visual.intro)}</p></header><div class="visualizer"><div class="trace-code"><header>JAVA EXECUTION</header><div class="trace-lines" id="traceLines">${lesson.visual.code.map((line,i)=>`<div class="trace-line" data-line="${i+1}"><b>${i+1}</b><span>${esc(line||" ")}</span></div>`).join("")}</div></div><div class="trace-state"><header>VISIBLE RUNTIME STATE</header><div class="trace-stage"><span id="traceCounter">READY</span><h3 id="traceTitle">Start the trace</h3><p id="traceText">Move line by line and connect the Java statement to its runtime effect.</p><div class="state-box" id="traceState">No state change yet.</div></div><div class="trace-controls"><button id="traceNext">Next step →</button><button id="traceAuto">▶ Auto run</button><button id="traceReset">Reset</button></div></div></div></section>`;}
function renderLesson(){if(!lesson){document.getElementById("lessonMain").innerHTML='<section class="lesson-section"><h1>Lesson data unavailable</h1><p>Return to the Java roadmap and choose another level.</p></section>';return;}document.title=`${lesson.title} — Java Level ${levelNumber} | CodeBhavya`;document.querySelector('meta[name="description"]').content=lesson.intro;const prev=allLevels.find(x=>x.n===levelNumber-1),next=allLevels.find(x=>x.n===levelNumber+1);document.getElementById("lessonMain").innerHTML=`<section class="lesson-hero" id="overview"><div><div class="breadcrumb"><a href="index.html">Java Course</a><span>›</span><span>Level ${String(levelNumber).padStart(2,"0")}</span></div><p class="eyebrow">${esc(lesson.part)} · LEVEL ${String(levelNumber).padStart(2,"0")}</p><h1>${esc(lesson.title)}</h1><p class="lead">${rich(lesson.intro)}</p><div class="lesson-meta"><span>Level ${levelNumber} of 24</span><span>${esc(lesson.difficulty)}</span><span>${esc(lesson.duration)}</span><span>${lesson.quiz.length} concept checks</span></div></div><aside class="outcome-card"><span>BY THE END, YOU CAN</span><h2>${esc(lesson.goal)}</h2><ul>${lesson.outcomes.map(x=>`<li>${rich(x)}</li>`).join("")}</ul></aside></section><nav class="jump-bar"><a href="#concepts">Concepts</a><a href="#examples">Examples</a>${lesson.visual?'<a href="#trace">Trace</a>':""}<a href="#revision">Revision</a><a href="#interview">Interview</a><a href="#practice">Practice</a><a href="#quiz">Check</a></nav><section class="lesson-section" id="concepts"><header class="section-heading"><span>CONCEPTS · MECHANISMS · TRADE-OFFS</span><h2>${esc(lesson.conceptTitle)}</h2><p>${rich(lesson.conceptIntro)}</p></header>${lesson.sections.map(sectionHtml).join("")}</section><section class="lesson-section" id="examples"><header class="section-heading"><span>RUNNABLE JAVA</span><h2>Examples Worth Tracing</h2><p>Predict the result before running each program. Then change one value and explain the new behaviour.</p></header>${examplesHtml()}</section>${visualHtml()}<section class="lesson-section" id="revision"><header class="section-heading"><span>QUICK REVISION</span><h2>Recall the Essential Ideas</h2></header><div class="revision-grid">${lesson.revision.map((x,i)=>`<div><b>${String(i+1).padStart(2,"0")}</b> ${rich(x)}</div>`).join("")}</div></section><section class="lesson-section" id="interview"><header class="section-heading"><span>INTERVIEW QUESTIONS</span><h2>Explain, Do Not Memorize</h2><p>Open each answer only after speaking your own version aloud.</p></header><div class="interview-list">${lesson.interview.map((x,i)=>`<details><summary>${i+1}. ${esc(x.q)}</summary><p>${rich(x.a)}</p></details>`).join("")}</div></section><section class="lesson-section" id="practice"><header class="section-heading"><span>PROGRAMMING PROBLEMS</span><h2>Apply the Concept Independently</h2><p>Use the CodeBhavya online compiler. Test normal input, a boundary value and one invalid case where relevant.</p></header><div class="practice-grid">${lesson.practice.map((x,i)=>`<article class="practice-card"><span>PROBLEM ${String(i+1).padStart(2,"0")}</span><h3>${esc(x.title)}</h3><p>${rich(x.task)}</p><details><summary>Show guidance</summary><p>${rich(x.hint)}</p></details></article>`).join("")}</div></section><section class="lesson-section" id="quiz"><header class="section-heading"><span>CONCEPT CHECK</span><h2>Verify Your Understanding</h2><p>Every answer shows the correct choice and the reason immediately.</p></header><div class="quiz-list" id="quizList"></div><div class="quiz-score" id="quizScore">Correct: 0 of 0 attempted · ${lesson.quiz.length} total</div></section><section class="lesson-section lesson-summary" id="summary"><div><p class="section-label">LEVEL ${String(levelNumber).padStart(2,"0")} SUMMARY</p><h2>${esc(lesson.summaryTitle)}</h2><ul class="summary-list">${lesson.summary.map(x=>`<li>${rich(x)}</li>`).join("")}</ul></div><aside class="complete-card"><span>COURSE CHECKPOINT</span><h3>Can you explain this level without reading?</h3><p>Complete the concept check and one programming problem before marking the lesson.</p><button type="button" id="completeLesson">Mark Level ${levelNumber} complete</button></aside></section><nav class="lesson-navigation">${prev?`<a href="lesson.html?level=${prev.n}">← Level ${prev.n}: ${esc(prev.title)}</a>`:'<a href="index.html">← Java roadmap</a>'}${next?`<a href="lesson.html?level=${next.n}">Level ${next.n}: ${esc(next.title)} →</a>`:'<a href="index.html">Review complete roadmap →</a>'}</nav>`;initExamples();initTrace();initQuiz();initCompletion();}
function initExamples(){document.querySelectorAll("[data-copy-example]").forEach(button=>button.addEventListener("click",async()=>{const code=lesson.examples[Number(button.dataset.copyExample)].code;try{await navigator.clipboard.writeText(code);button.textContent="✓ Copied"}catch(_){const area=document.createElement("textarea");area.value=code;document.body.append(area);area.select();document.execCommand("copy");area.remove();button.textContent="✓ Copied"}setTimeout(()=>button.textContent="Copy code",1300)}));}
function initTrace(){if(!lesson.visual)return;let at=-1,timer=null;const steps=lesson.visual.steps,lines=[...document.querySelectorAll(".trace-line")],next=document.getElementById("traceNext"),auto=document.getElementById("traceAuto");function draw(){const step=steps[at];document.getElementById("traceCounter").textContent=step?`STEP ${at+1} OF ${steps.length}`:"READY";document.getElementById("traceTitle").textContent=step?.title||"Start the trace";document.getElementById("traceText").textContent=step?.text||"Move line by line and connect the Java statement to its runtime effect.";document.getElementById("traceState").textContent=step?.state||"No state change yet.";lines.forEach(line=>line.classList.toggle("active",Number(line.dataset.line)===step?.line));lines.find(line=>line.classList.contains("active"))?.scrollIntoView({block:"nearest"});next.disabled=at>=steps.length-1;}next.addEventListener("click",()=>{if(at<steps.length-1)at++;draw()});document.getElementById("traceReset").addEventListener("click",()=>{clearInterval(timer);timer=null;auto.textContent="▶ Auto run";at=-1;draw()});auto.addEventListener("click",()=>{if(timer){clearInterval(timer);timer=null;auto.textContent="▶ Auto run";return;}auto.textContent="Ⅱ Pause";timer=setInterval(()=>{if(at>=steps.length-1){clearInterval(timer);timer=null;auto.textContent="▶ Auto run";return;}at++;draw();},1100)});draw();}
function initQuiz(){const host=document.getElementById("quizList"),score=document.getElementById("quizScore");let correct=0,attempted=0;lesson.quiz.forEach((item,i)=>{const card=document.createElement("article");card.className="quiz-card";card.innerHTML=`<span>CHECK ${String(i+1).padStart(2,"0")}</span><h3>${esc(item.q)}</h3><div class="quiz-options"></div><p class="quiz-feedback" aria-live="polite"></p>`;const options=card.querySelector(".quiz-options"),feedback=card.querySelector(".quiz-feedback");item.options.forEach((option,j)=>{const button=document.createElement("button");button.type="button";button.textContent=option;button.addEventListener("click",()=>{if(card.dataset.answered)return;card.dataset.answered="true";attempted++;if(j===item.answer)correct++;[...options.children].forEach((b,k)=>{if(k===item.answer)b.classList.add("correct");else if(k===j)b.classList.add("wrong");b.disabled=true});feedback.textContent=`${j===item.answer?"Correct.":`Selected: ${option}. Correct: ${item.options[item.answer]}.`} ${item.why}`;score.textContent=`Correct: ${correct} of ${attempted} attempted · ${lesson.quiz.length} total`;});options.append(button)});host.append(card)});}
function initCompletion(){const button=document.getElementById("completeLesson");function draw(){const done=readDone().includes(levelNumber);button.classList.toggle("done",done);button.textContent=done?`✓ Level ${levelNumber} completed`:`Mark Level ${levelNumber} complete`;button.setAttribute("aria-pressed",done);document.getElementById("sidebarStatus").textContent=done?"Completed":"Not completed";}button.addEventListener("click",()=>{let values=readDone();values=values.includes(levelNumber)?values.filter(x=>x!==levelNumber):[...new Set([...values,levelNumber])];localStorage.setItem(JAVA_KEY,JSON.stringify(values));draw()});draw();}
function initShell(){const button=document.getElementById("lessonMenu"),sidebar=document.getElementById("lessonSidebar"),shade=document.getElementById("sidebarShade"),navButton=document.getElementById("navToggle"),siteNav=document.getElementById("siteNav");function openSidebar(open){sidebar.classList.toggle("open",open);button.setAttribute("aria-expanded",open);shade.hidden=!open}button.addEventListener("click",()=>openSidebar(!sidebar.classList.contains("open")));shade.addEventListener("click",()=>openSidebar(false));sidebar.addEventListener("click",e=>{if(e.target.closest("a"))openSidebar(false)});navButton.addEventListener("click",()=>{const open=siteNav.classList.toggle("open");navButton.setAttribute("aria-expanded",open);navButton.textContent=open?"×":"☰"});addEventListener("scroll",()=>{const max=document.documentElement.scrollHeight-innerHeight;document.getElementById("readingBar").style.width=`${max>0?Math.min(100,scrollY/max*100):0}%`},{passive:true});document.getElementById("year").textContent=new Date().getFullYear();}
renderSidebar();renderLesson();initShell();
