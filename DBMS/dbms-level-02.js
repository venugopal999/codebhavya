(function(){
"use strict";
const STORAGE_KEY="codebhavya.dbms.course.progress.v1";
const schemaLevels={external:{tag:"EXTERNAL / VIEW LEVEL",title:"What a particular user or application is allowed to see",text:"A database can have many external schemas. Each presents only relevant fields and may rename or derive data for its audience.",contains:"Views, selected attributes, derived values and user-specific representations.",hides:"The rest of the logical database and every physical storage detail."},conceptual:{tag:"CONCEPTUAL / LOGICAL LEVEL",title:"The complete logical structure of the database",text:"The conceptual schema unifies all entities, attributes, relationships and integrity constraints without describing disk storage.",contains:"All logical record types, relationships, keys and business constraints.",hides:"File placement, pages, indexes, compression and device details."},internal:{tag:"INTERNAL / PHYSICAL LEVEL",title:"How records are represented and accessed in storage",text:"The internal schema describes structures chosen to store and retrieve the logical data efficiently.",contains:"Files, pages, record layouts, indexes, partitions and access paths.",hides:"Low-level details from logical designers, applications and end users."}};
function initSchemaExplorer(){const buttons=Array.from(document.querySelectorAll("[data-schema-level]")),detail=document.getElementById("schemaLevelDetail");if(!detail)return;function render(key){const level=schemaLevels[key];detail.innerHTML="<span>"+level.tag+"</span><h3>"+level.title+"</h3><p>"+level.text+"</p><div class=\"schema-detail-columns\"><div><b>Contains</b><p>"+level.contains+"</p></div><div><b>Hides</b><p>"+level.hides+"</p></div></div>";}buttons.forEach(function(button){button.addEventListener("click",function(){buttons.forEach(function(item){item.setAttribute("aria-selected",String(item===button));});render(button.dataset.schemaLevel);});});render("external");}
const changes={index:{level:"internal",tag:"PHYSICAL DATA INDEPENDENCE",title:"Only the internal schema changes",text:"An index is an access path. Tables and user views keep the same logical meaning; applications should continue to work."},storage:{level:"internal",tag:"PHYSICAL DATA INDEPENDENCE",title:"Storage can change below the conceptual schema",text:"Moving database files to SSD storage changes physical placement, not the entities, attributes or user-facing views."},attribute:{level:"conceptual",tag:"LOGICAL DATA INDEPENDENCE",title:"The conceptual schema gains an attribute",text:"Existing external views can remain unchanged while a new or updated view exposes student_email where required."},split:{level:"conceptual",tag:"LOGICAL DATA INDEPENDENCE",title:"The logical organization changes",text:"Mappings or views can preserve an older application interface even after address data moves into a related table."}};
function initChangeSimulator(){const buttons=Array.from(document.querySelectorAll("[data-change]")),result=document.getElementById("changeResult"),nodes=Array.from(document.querySelectorAll(".impact-node"));buttons.forEach(function(button){button.addEventListener("click",function(){buttons.forEach(function(item){item.classList.toggle("is-active",item===button);});const change=changes[button.dataset.change];nodes.forEach(function(node){const logical=node.textContent.toLowerCase().includes("conceptual");const internal=node.textContent.toLowerCase().includes("internal");node.classList.toggle("is-changing",(change.level==="conceptual"&&logical)||(change.level==="internal"&&internal));});result.innerHTML="<span>"+change.tag+"</span><h3>"+change.title+"</h3><p>"+change.text+"</p>";});});}
const scenarios={college:{tag:"RECOMMENDED PRIMARY MODEL",title:"Relational model",text:"Students, courses, enrolments and results have well-defined relationships and constraints. SQL supports both transactions and reporting.",fit:"Strong keys, joins, integrity and structured queries",caution:"Use careful normalization and indexes as the system grows"},filesystem:{tag:"RECOMMENDED MODEL",title:"Hierarchical model",text:"Each employee has one reporting parent, so a tree expresses the dominant relationship naturally.",fit:"Fast traversal of a stable parent–child hierarchy",caution:"Matrix reporting or multiple parents weaken the simple tree fit"},social:{tag:"RECOMMENDED MODEL",title:"Graph / network-oriented model",text:"People and connections form many-to-many paths. Relationship traversal is central to the workload.",fit:"Flexible connected data and multi-hop traversal",caution:"A relational model may still be better when tabular reporting dominates"},session:{tag:"RECOMMENDED MODEL",title:"Key–value model",text:"A known session identifier retrieves one short-lived value directly, with little need for joins or ad-hoc analysis.",fit:"Very fast lookup by a unique key",caution:"Keep authoritative long-term user records elsewhere"},catalog:{tag:"RECOMMENDED MODEL",title:"Document model",text:"Products can share core fields while category-specific attributes vary. One product can be read as one aggregate document.",fit:"Flexible nested records and evolving attributes",caution:"Plan duplication, validation and cross-product reporting carefully"}};
function initModelSelector(){const select=document.getElementById("scenarioSelect"),result=document.getElementById("modelRecommendation");if(!select||!result)return;function render(){const item=scenarios[select.value];result.innerHTML="<span>"+item.tag+"</span><h3>"+item.title+"</h3><p>"+item.text+"</p><div class=\"model-reasoning\"><div><b>Why it fits</b><small>"+item.fit+"</small></div><div><b>Design caution</b><small>"+item.caution+"</small></div></div>";}select.addEventListener("change",render);render();}
const tiers={one:{tag:"ONE-TIER / LOCAL ARCHITECTURE",title:"User, application and database are on one system",text:"Often used for learning, local development or a small standalone tool.",nodes:[["User + application + DBMS","One machine/process boundary"]],benefits:["Simple setup","No network round-trip","Useful for local practice"]},two:{tag:"TWO-TIER / CLIENT–SERVER",title:"A client application communicates directly with the database server",text:"The client contains presentation and often significant business logic, then sends SQL or database calls to the server.",nodes:[["Desktop client","UI + some business logic"],["Database server","DBMS + database"]],benefits:["Direct communication","Suitable for controlled LAN tools","Fewer deployment layers"]},three:{tag:"THREE-TIER / WEB ARCHITECTURE",title:"A service layer stands between the user interface and database",text:"The application tier validates requests, applies business rules, manages authorization and communicates with the DBMS.",nodes:[["Presentation tier","Browser or mobile UI"],["Application tier","API + business logic"],["Data tier","DBMS + database"]],benefits:["Centralized rules","Better security boundary","Scalable maintenance"]}};
function initTiers(){const buttons=Array.from(document.querySelectorAll("[data-tier]")),display=document.getElementById("tierDisplay");if(!display)return;function render(key){const tier=tiers[key];display.innerHTML="<span>"+tier.tag+"</span><h3>"+tier.title+"</h3><p>"+tier.text+"</p><div class=\"tier-flow\">"+tier.nodes.map(function(node,index){return(index?"<b class=\"tier-arrow\">⇄</b>":"")+"<div class=\"tier-node\"><b>"+node[0]+"</b><small>"+node[1]+"</small></div>";}).join("")+"</div><div class=\"tier-benefits\">"+tier.benefits.map(function(item){return"<span>"+item+"</span>";}).join("")+"</div>";}buttons.forEach(function(button){button.addEventListener("click",function(){buttons.forEach(function(item){item.setAttribute("aria-selected",String(item===button));});render(button.dataset.tier);});});render("one");}
const feedback=["The internal level describes physical representation and access paths.","An index changes storage access without changing the logical tables.","A schema is the blueprint; an instance is the current collection of stored values.","The relational model represents data in relations (tables) connected using keys.","The application tier normally contains reusable business rules and communicates with the data tier."];
function initChecks(){const checks=Array.from(document.querySelectorAll(".concept-check")),score=document.getElementById("conceptScore"),answered=new Map();function update(){let correct=0;answered.forEach(function(value){if(value)correct+=1;});if(score)score.textContent="Answered correctly: "+correct+" of "+checks.length;}checks.forEach(function(check,index){const answer=check.dataset.answer,output=check.querySelector(".check-feedback"),buttons=Array.from(check.querySelectorAll("[data-option]"));buttons.forEach(function(button){button.addEventListener("click",function(){const right=button.dataset.option===answer;answered.set(index,right);buttons.forEach(function(item){item.classList.remove("is-correct","is-wrong");if(item.dataset.option===answer)item.classList.add("is-correct");});if(!right)button.classList.add("is-wrong");output.innerHTML=right?"<strong>Correct.</strong> "+feedback[index]:"Not quite. Review the highlighted answer: "+feedback[index];update();});});});}
function readProgress(){try{const value=JSON.parse(localStorage.getItem(STORAGE_KEY)||"[]");return Array.isArray(value)?value.map(Number):[];}catch(error){return[];}}
function initCompletion(){const button=document.getElementById("completeLessonButton"),status=document.getElementById("lessonSidebarStatus");if(!button)return;let completed=readProgress();function render(){const done=completed.indexOf(2)!==-1;button.classList.toggle("is-complete",done);button.setAttribute("aria-pressed",String(done));button.textContent=done?"✓ Level 2 completed":"Mark Level 2 complete";if(status){status.textContent=done?"Level 2 completed":"Not completed";status.parentElement.classList.toggle("is-complete",done);}}button.addEventListener("click",function(){const index=completed.indexOf(2);if(index===-1)completed.push(2);else completed.splice(index,1);completed.sort(function(a,b){return a-b;});try{localStorage.setItem(STORAGE_KEY,JSON.stringify(completed));}catch(error){}render();});render();}
function initReading(){const bar=document.getElementById("lessonReadingBar"),links=Array.from(document.querySelectorAll(".lesson-section-link")),sections=links.map(function(link){return document.querySelector(link.getAttribute("href"));}).filter(Boolean);function update(){const max=document.documentElement.scrollHeight-window.innerHeight,percent=max>0?Math.min(100,Math.max(0,window.scrollY/max*100)):0;if(bar)bar.style.width=percent+"%";}window.addEventListener("scroll",update,{passive:true});update();if("IntersectionObserver"in window){const observer=new IntersectionObserver(function(entries){entries.forEach(function(entry){if(!entry.isIntersecting)return;links.forEach(function(link){link.classList.toggle("active",link.getAttribute("href")==="#"+entry.target.id);});});},{rootMargin:"-20% 0px -70% 0px"});sections.forEach(function(section){observer.observe(section);});}}
function init(){initSchemaExplorer();initChangeSimulator();initModelSelector();initTiers();initChecks();initCompletion();initReading();}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init();
})();
